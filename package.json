{
  "name": "aqara-cameras-bridge",
  "description": "Aqara Cameras Local RTSP & MQTT Integration",
  "version": "1.3.1",
  "private": true,
  "scripts": {
    "start": "npm --prefix app start",
    "dev": "npm --prefix app run dev",
    "build": "npm --prefix app run build",
    "rtsp": "npm --prefix app run rtsp",
    "capture": "npm --prefix app run capture",
    "login": "npm --prefix app run login",
    "setup": "npm --prefix app run setup"
  }
}
